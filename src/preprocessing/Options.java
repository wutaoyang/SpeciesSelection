package preprocessing;

/**
 * Enum representing options for outputting results from a Species Selection 
 * problem species analysis
 * @author mre16utu
 */
public enum Options {
    ALL,
    NONE,
    FINAL
}
